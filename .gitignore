.venv
log*.*
__pycache__
.DS_Store
.idea
**/target/

# Vendor license signing keys — NEVER commit. Anyone with the seed can mint
# licenses. Keep the real key in a password manager / HSM, not in the repo.
licensing/keys/
*.seed
*.seed.hex

licensing

# Other random files not needed
infra
java
native
native_handler
tests
test_files