[build-system]
requires = ["maturin>=1.9,<2.0"]
build-backend = "maturin"

[project]
name = "platypus"
requires-python = ">=3.9"
dynamic = ["version"]

[tool.maturin]
module-name = "platypus"
features = ["python"]
